# simple-mongodb-seeder
- Install dependencies:
  ```
  npm install
  # OR
  yarn install
  ```
- Make sure MongoDB service has been started. Then, execute this command
 ```
  mongosh < seeder.js
  ```
